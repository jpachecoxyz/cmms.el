;;; Directory Local Variables
;;; For more information see (info "(emacs) Directory Variables")

((nil . ((eval . (add-to-list 'load-path default-directory))
         (eval . (require 'cmms))
         (cmms-company-name . "Castlight de Mexico"))))
